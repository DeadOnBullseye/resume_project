function onScroll() {
  var nav = document.querySelector('.header');
  addEventListener('scroll', function() {
    if (window.innerWidth > 600) {
      if (document.documentElement.scrollTop > 100) {
        console.log("change color");
        nav.style.backgroundColor = "rgba(15,15,15,1)"
        nav.style.boxShadow = '0 3px 5px rgba(0,0,0,0.6)';
        nav.style.transition = 'all .2s ease-out';
        nav.style.padding = '0';
        $('.line').removeClass('scrollDown');
      } else {
        nav.style.backgroundColor = "rgba(0,0,0,0)"
        nav.style.boxShadow = '0 3px 5px rgba(0,0,0,0)';
        nav.style.padding = '20px 0';
        $('.line').addClass('scrollDown');
      }
    } else {
      nav.style.backgroundColor = "rgba(15,15,15,1)"
      nav.style.padding = '0';
      $('.line').removeClass('scrollDown');
    }
  });
}

$('.menu-btn').on('click', event => {
  $(event.currentTarget).toggleClass('menu-btn__click');
  $('.menu').toggleClass('menu__click');
  $('.logo').toggleClass('menu__click___logo');
  $('body').toggleClass('stop-scrolling');
});
