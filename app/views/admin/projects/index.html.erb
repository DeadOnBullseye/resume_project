<%= render 'partials/header_admin' %>

<div class="main" style="box-shadow: none; height: 100%; display: flex; flex-direction: column; justify-content: center; align-content: center;">
  <div class="row" style="">
    <div class="content" style="display: flex; justify-content: center; align-items: center; flex-direction: column;">
      <div class="content__heading-2" style="align-self: center;">Manage Projects</div>
      <%= link_to "Create", new_admin_project_path, class:'btn' %>
      <table>
        <tr>
          <th>Project Title</th>
          <th>Project Category</th>
          <th>Date Created</th>
          <th>Actions</th>
        </tr>
        <% @projects.each do |project| %>
          <tr>
            <th><%= project.title %></th>
            <th><%= project.category.name %></th>
            <th><%= project.created_at.to_time.strftime('%B %e at %l:%M %p') %></th>
            <th><%= link_to "Edit", edit_admin_project_path(project), class:'btn' %> <%= link_to "Delete", admin_project_path(project), class:'btn', method: :delete, data: {confirm: 'Are you sure?'} %></th>
          </tr>
        <% end %>
      </table>

    </div>
  </div>
</div>
